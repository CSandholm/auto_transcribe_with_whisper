FOR OFFLINE USE!

It's required to have ffmpeg downloaded on the system.

When installing a new openai whisper you have to change the path for vocab.bpe and encoder.json.
In the virtual environment, or Python interpreter: Lib/site-packages/tiktoken-ext/openai_public , set the path to local vocab.bpe and encoder.json.